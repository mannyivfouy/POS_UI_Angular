export interface UserStats {
  totalUser: number;
  activeUser: number;
  inactiveUser: number;
  data: any;
}

export interface ProductStats {
  totalProduct: number;
  activeProduct: number;
  inactiveProduct: number;
  data: any;
}
